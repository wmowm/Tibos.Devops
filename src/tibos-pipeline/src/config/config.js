// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
  theme: {
    color: '#096dd9', //换成任何你喜欢的颜色，支持 hex 色值
    mode: 'dark',
  },
  multiPage: true,
  asyncRoutes: true,
  animate: {
    disabled: false,
    name: 'lightSpeed',
    direction: 'left'
  }
}